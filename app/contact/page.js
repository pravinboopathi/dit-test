"use client"; // Required for client-side rendering

import React from "react";

const ContactPage = () => {
  const bgColor = "#f0f0f0"; // Set a default color if needed

  return (
    <div style={{ backgroundColor: bgColor, padding: "20px" }}>
      <h1>Contact Page</h1>
    </div>
  );
};

export default ContactPage;
